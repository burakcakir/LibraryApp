<UserControl x:Class="KutuphaneTakip.UserController.ucLibraryApp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KutuphaneTakip.UserController"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0">
            <DataGrid BorderBrush="{x:Null}" BorderThickness="0" Background="#FFD34040">
                
            </DataGrid>
        </Border>

        <Border Background="White" Grid.Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{DynamicResource userKontrolKitapEkleButon}" Content="Kitap Ekle"></Button>
                <Button Style="{DynamicResource userKontrolKitapEkleButon}" Content="Kitap Ekle"></Button>
            </StackPanel>
        </Border>
        
        
    </Grid>
</UserControl>
